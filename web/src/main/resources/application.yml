server:
  port: 80
spring:
  thymeleaf:
    cache: false
    mode: HTML5
    encoding: utf-8
  application:
    name: WEB
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:890/eureka/
# 自定义
app:
  host: http://localhost
  